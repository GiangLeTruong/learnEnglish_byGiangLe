<!-- Breadcrumb Start -->
<div class="breadcrumb-wrap">
    <div class="container">
        <ul class="breadcrumb">
            <li class="breadcrumb-item"><a href="#">Trang chủ</a></li>
            <li class="breadcrumb-item"><a href="#">Thao tác</a></li>
            <li class="breadcrumb-item" active><a href="#">Chi tiết từ vựng</a></li>
        </ul>
    </div>
</div>
<!-- Breadcrumb End -->


<!-- Product Detail Start -->
<div class="product-detail">
    <div class="container">
        <div class="row">
            <div class="col-lg-9">
                {{#with detailVocabulary}}
                <div class="row align-items-center product-detail-top">
                    <div class="col-md-5">
                        <div class="product-slider-single">
                            {{#with Medium}}
                            <img src="{{imagePath}}" alt="{{../word}}">
                            {{/with}}

                        </div>
                    </div>
                    <div class="col-md-7">
                        <div class="product-content">
                            <div class="title">
                                <h1>{{word}} </h1><span class="fs-3">({{type}})</span>

                                <h3>/{{pronunciation}}/
                                    {{#with Medium}}
                                    <button onclick="playAudio('audio-{{id}}')">🔊</button>
                                    <audio id="audio-{{id}}" src="{{audioPath}}"></audio>
                                    {{/with}}
                                </h3>

                                {{#with Certificate}}
                                <h2>[ <span class="text-danger ">{{name}}</span>-
                                    {{/with}}
                                    {{#with Category}}
                                    <span class="text-info ">{{name}}</span>
                                    {{/with}}
                                    ]
                                </h2>
                            </div>

                            {{{renderStars difficulty}}}

                            <div class="price">Meaning:</div>
                            <div class="details">
                                <p>
                                    {{definition}}
                                </p>
                            </div>

                            <div class="action">
                                <a href="#" onclick="updateLearnedState('{{id}}')"><i class="fa fa-paper-plane"></i></a>
                                <input type="checkbox" id="is_Learned_{{id}}" name="is_Learned" value="true" {{#if
                                    isLearned}}checked{{/if}}>
                                <label for="is_Learned_{{id}}"> Learned</label><br>
                            </div>
                            <br>
                            <div class="action"><a href="/list"><i class="fa fa-rotate-left"></i></a> Về lại danh sách
                                từ
                                vựng.</div>


                        </div>
                    </div>
                </div>

                <div class="row product-detail-bottom">
                    <div class="col-lg-12">
                        <ul class="nav nav-pills nav-justified">
                            <li class="nav-item">
                                <a class="nav-link active" data-toggle="pill" href="#meaning_vi">Vietnamese-Meaning</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" data-toggle="pill" href="#examples">Examples</a>
                            </li>
                        </ul>

                        <div class="tab-content">
                            <div id="meaning_vi" class="container tab-pane active"><br>
                                <h4>Nghĩa tiếng Việt của từ</h4>
                                <p>
                                    {{meaning_vi}}
                                </p>
                            </div>
                            <div id="examples" class="container tab-pane fade"><br>
                                <h4>Some examples</h4>
                                {{#each Examples}}
                                <p>
                                    {{sentence}}
                                </p>
                                {{/each}}
                            </div>

                        </div>
                    </div>
                </div>
                {{/with}}
                <div class="container">
                    <div class="section-header">
                        <h3>Related Vocabularies</h3>
                        <p>
                            Some related vocabulary to help you learn more effectivly.
                        </p>
                    </div>
                </div>


                {{#if relatedVocabularies.length}}


                <div class="row align-items-center product-slider product-slider-3">

                    {{#each relatedVocabularies}}
                    <div class="col-lg-4">
                        {{> vocabulary }}
                    </div>
                    {{/each}}
                </div>


                {{else}}
                <div class="container text-center">
                    <h3 class="text-danger"> (Empty) </h3>
                </div>

                {{/if}}
            </div>




            {{> right-column }}
        </div>
    </div>
</div>
<!-- Product Detail End -->